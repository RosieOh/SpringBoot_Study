server:
  port: 8085

spring:
  mvc:
    view:
      prefix: /WEB-INF/view
      suffix: .jsp

      # 중복기재 하지 않고 같은 항목일 경우 같이 추가 가능
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/edumon
    username: root
    password: 1234

mybatis:
  type-aliases-package: com.chunjae
  mapper-locations: mappers/**/*.xml
